import 'package:flutter/material.dart';

class CalcImc extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Calculadora"),
      ),
      body: Center(
        child: Text('aqui entrara el codigo de la calculadora de IMC'),
      ),
    );
  }
}